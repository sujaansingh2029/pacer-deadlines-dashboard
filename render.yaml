services:
  - type: web
    name: pacer-deadlines-dashboard
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: DATABASE_URL
        fromDatabase:
          name: pacer-deadlines-db
          property: connectionString
      - key: APP_BASE_URL
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: CRON_SECRET
        generateValue: true
      - key: SESSION_SECRET
        generateValue: true
      - key: DASHBOARD_PASSWORD
        sync: false

  - type: cron
    name: pacer-deadlines-sync
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm run sync
    schedule: "0 13,16,19 * * *"
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: DATABASE_URL
        fromDatabase:
          name: pacer-deadlines-db
          property: connectionString
      - key: APP_BASE_URL
        sync: false
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: CRON_SECRET
        generateValue: true
      - key: SESSION_SECRET
        generateValue: true

databases:
  - name: pacer-deadlines-db
    plan: basic-256mb
